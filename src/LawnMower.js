const CoreFunctions = require('./CoreFunctions');



module.exports = class LawnMower extends CoreFunctions {

    // Currently the lawnmower behavior is exactly the same as the CoreFunctions
    // but due to this structure it can always override parts of the default behavior
    // defined in CoreFunctions.js or have specific behavior.  


  }
